/**
 * Crea una función que reciba una lista de personas y muestre
 * por consolas sus nombres.
 */
const peopleList = (arrayPersonas) => arrayPersonas.map(({ nombre }) => nombre);

export default peopleList;